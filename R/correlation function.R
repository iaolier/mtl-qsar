### r^2 correlation function

 # <- function(truth, preds) {
 #  correlation <- (sum((truth-mean(truth))*(preds-mean(preds))))/sqrt((sum((truth-mean(truth))^2)*sum((preds-mean(preds))^2)))
 #  return(correlation)
 #  }
 #  
  


#correlation(x,y)
